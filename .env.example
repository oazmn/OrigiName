# Originame — environment variables
#
# This app runs fully client-side and requires no API keys.
# The name database is bundled at build time (src/lib/nameDatabase.json).
# No .env.local file is needed to run or deploy this project.

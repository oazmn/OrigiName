# Copy this file to .env.local and fill in real values.
# NEVER commit .env.local to version control.

# Required: Anthropic API key for Claude name generation fallback.
# Generate one at https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=

# Optional: set to true to skip Claude API calls in local development.
MOCK_AI=false
